#pragma once

namespace config::features {
    
    // Graphics
    constexpr bool enable_2d = true;
    constexpr bool enable_3d = true;

    // Extras
    constexpr bool enable_fonts = true;
    constexpr bool enable_audio = true;

    // Input
    constexpr bool enable_keyboard = true;
    constexpr bool enable_mouse = true;
    constexpr bool enable_joypad = true;

}